This is a test document for the knowledge base.

It contains some simple text that should be easily processed for embedding.

The test has multiple paragraphs to ensure the chunking logic works correctly.

This document is intentionally kept simple to avoid any complexity with PDF parsing or other file format issues.

Let's see if this works with the knowledge base system. 